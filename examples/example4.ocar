sage x;
using work;
using work.overkill.why;
using because;

start
	for(int i = 0; i < j; i++):
		i++;
		j++;
		++i;
		++j;
		
		int z = i++ + u-- * ++l / --z;
	end
end

public var x = 0;
private int y;